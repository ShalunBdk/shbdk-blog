# Alexander Shalin - DevOps Blog

Личный блог с заметками, сниппетами и проектами. Статичный сайт, генерируемый из Markdown файлов.

## 🚀 Быстрый старт

```bash
# Установить зависимости
npm install

# Собрать сайт
npm run build

# Разработка с отслеживанием изменений
npm run dev

# Очистить dist/
npm run clean
```

## 📁 Структура проекта

```
blog/
├── content/              # Контент в Markdown
│   ├── posts/           # Посты блога
│   ├── snippets/        # Сниппеты кода
│   └── projects/        # Описания проектов
├── templates/           # HTML шаблоны
│   ├── base.html       # Базовый шаблон
│   ├── home.html       # Главная страница
│   ├── post.html       # Шаблон поста
│   ├── snippets.html   # Страница сниппетов
│   └── projects.html   # Страница проектов
├── src/
│   └── styles/         # CSS файлы
├── scripts/
│   └── build.js        # Скрипт сборки
├── public/             # Статичные файлы (копируются в dist/)
├── dist/               # Собранный сайт (gitignore)
└── package.json
```

## ✍️ Создание контента

### Новый пост

Создайте файл `content/posts/название-поста.md`:

```markdown
---
title: Заголовок поста
date: 2025-01-15
tags: [Docker, DevOps, Production]
excerpt: Краткое описание поста
---

## Введение

Ваш контент здесь...
```

### Новый сниппет

Создайте файл `content/snippets/название-сниппета.md`:

```markdown
---
title: Docker cleanup команды
category: Docker
tags: [Docker, Cleanup]
---

Описание сниппета...

\`\`\`bash
docker system prune -a --volumes -f
\`\`\`
```

### Новый проект

Создайте файл `content/projects/название-проекта.md`:

```markdown
---
title: GLPI Inventory System
icon: 📦
tech: [Docker, Linux, GLPI, Windows]
github: https://github.com/ShalunBdk
demo: https://example.com
---

Описание проекта...
```

## 🔧 Технологии

- **Node.js** - runtime
- **markdown-it** - парсинг Markdown
- **gray-matter** - frontmatter метаданные
- **highlight.js** - подсветка синтаксиса
- **fs-extra** - работа с файлами

## 📝 Frontmatter поля

### Посты
- `title` - заголовок (обязательно)
- `date` - дата публикации (обязательно)
- `tags` - массив тегов
- `excerpt` - краткое описание

### Сниппеты
- `title` - заголовок (обязательно)
- `category` - категория (Linux, Docker, Python, etc.)
- `tags` - массив тегов
- `language` - язык кода (bash, python, yaml, etc.)

### Проекты
- `title` - название (обязательно)
- `icon` - эмодзи иконка
- `tech` - массив технологий
- `github` - ссылка на GitHub
- `demo` - ссылка на демо

## 🌐 Деплой

После сборки (`npm run build`) папка `dist/` содержит готовый статичный сайт.

Можно задеплоить на:
- GitHub Pages
- Netlify
- Vercel
- Любой статичный хостинг

## 📄 Лицензия

MIT © Alexander Shalin
