---
title: Базовая настройка безопасности Linux Server
date: 2025-05-26
tags: [🐧 Linux, 🔒 Security, 🛡️ UFW, 🚫 Fail2Ban]
excerpt: Пошаговое руководство по базовой настройке безопасности Linux сервера - UFW, Fail2Ban, SSH-ключи
---

## ШАГ 1: Настройка базовой безопасности сервера

### 1. Обновление системы

```bash
sudo apt update && sudo apt upgrade -y
```

### 2. Установка базовых утилит

```bash
sudo apt install ufw curl wget git fail2ban -y
```

### 3. Настройка UFW (фаервол)

Заблокируем все входящие подключения, разрешим только SSH, HTTP, HTTPS и необходимые порты.

```bash
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow OpenSSH
sudo ufw allow http
sudo ufw allow https

# Пример: если будешь использовать порт 8000
sudo ufw allow 8000/tcp

sudo ufw enable
```

Проверка статуса:

```bash
sudo ufw status
```

### 4. Настройка Fail2Ban (защита от брутфорса)

Fail2Ban автоматически блокирует IP-адреса после нескольких неудачных попыток входа.

```bash
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

Создайте файл конфигурации `/etc/fail2ban/jail.local`:

```bash
sudo nano /etc/fail2ban/jail.local
```

Пример минимальной конфигурации:

```ini
[DEFAULT]
bantime = 86400
findtime = 600
maxretry = 5

[sshd]
enabled = true
```

## ШАГ 2: Создание безопасного пользователя

> ⚠️ **Важно:** Не работайте под root! Создайте своего пользователя с правами sudo.

```bash
adduser your_username
usermod -aG sudo your_username
su - your_username
```

Замените `your_username` на желаемое имя пользователя.

## ШАГ 3: Настройка SSH-ключей и отключение входа по паролю

### Создание SSH-ключа локально

На вашем локальном компьютере (Windows/Linux/macOS):

```powershell
ssh-keygen
```

### Загрузка публичного ключа на сервер

Скопируйте содержимое вашего публичного ключа (`~/.ssh/id_ed25519.pub` или `%USERPROFILE%/.ssh/id_ed25519.pub` на Windows) в файл на сервере:

```bash
mkdir -p ~/.ssh
nano ~/.ssh/authorized_keys
# Вставьте публичный ключ и сохраните
```

Установите правильные права доступа:

```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### Отключение входа по паролю

Откройте конфигурацию SSH:

```bash
sudo nano /etc/ssh/sshd_config
```

Измените следующие параметры:

```conf
PasswordAuthentication no
PermitRootLogin no
ChallengeResponseAuthentication no
UsePAM no
```

Перезапустите SSH:

```bash
sudo systemctl restart sshd
```

> ⚠️ **Внимание:** Обязательно проверьте, что вы можете зайти через SSH-ключ в **новом окне терминала**, прежде чем закрывать текущую сессию! Иначе можете потерять доступ к серверу.

## Проверка безопасности

После выполнения всех шагов проверьте:

1. **UFW активен** и разрешены только нужные порты:
   ```bash
   sudo ufw status numbered
   ```

2. **Fail2Ban работает**:
   ```bash
   sudo fail2ban-client status
   sudo fail2ban-client status sshd
   ```

3. **Вход через SSH-ключ работает**, вход по паролю заблокирован

4. **Root-доступ отключен**, работаете от обычного пользователя с sudo

## Дополнительные рекомендации

- Регулярно обновляйте систему: `sudo apt update && sudo apt upgrade`
- Настройте автоматические обновления безопасности
- Используйте сложные SSH-ключи (ed25519 или RSA 4096)
- Периодически просматривайте логи: `sudo journalctl -u ssh`
- Настройте мониторинг сервера (например, через Prometheus + Grafana)
