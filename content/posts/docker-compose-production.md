---
title: Docker Compose для production
date: 2025-01-15
tags: [🐳 Docker, ⚙️ DevOps, 🏭 Production]
excerpt: Как правильно настроить docker-compose для продакшена с управлением секретами, health checks и resource limits
---

## Введение

При переносе Docker приложений в production окружение важно правильно настроить docker-compose файл. В этой статье рассмотрим основные best practices для продакшена.

> **Важно:** Никогда не используйте в продакшене конфигурацию, предназначенную для разработки. Это может привести к серьёзным проблемам с безопасностью и производительностью.

## Настройка безопасности

Первое, что нужно сделать — это правильно настроить управление секретами и переменными окружения.

### Использование Docker Secrets

Вместо хранения паролей в переменных окружения используйте Docker Secrets:

```yaml
version: '3.8'

services:
  app:
    image: myapp:latest
    secrets:
      - db_password
      - api_key
    environment:
      DB_PASSWORD_FILE: /run/secrets/db_password

secrets:
  db_password:
    file: ./secrets/db_password.txt
  api_key:
    external: true
```

### Запуск контейнеров без root

Всегда указывайте пользователя для запуска контейнера:

```yaml
services:
  app:
    image: myapp:latest
    user: "1000:1000"
    security_opt:
      - no-new-privileges:true
```

## Управление ресурсами

Ограничивайте потребление CPU и памяти для предотвращения проблем:

```yaml
services:
  app:
    image: myapp:latest
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
```

## Health Checks

Health checks позволяют Docker автоматически перезапускать неработающие контейнеры:

```yaml
services:
  app:
    image: myapp:latest
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
```

## Логирование

Настройте ротацию логов, чтобы они не заполнили диск:

```yaml
services:
  app:
    image: myapp:latest
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

### Основные рекомендации:

- Используйте конкретные версии образов вместо `latest`
- Настройте networks для изоляции сервисов
- Используйте named volumes для персистентных данных
- Регулярно обновляйте образы и проверяйте на уязвимости
- Настройте мониторинг и алертинг

## Заключение

Правильная настройка Docker Compose для production — это комплексная задача, включающая безопасность, управление ресурсами, логирование и мониторинг. Следуя этим рекомендациям, вы сможете создать стабильное и безопасное окружение для ваших приложений.

> 💡 **Совет:** Всегда тестируйте конфигурацию в staging окружении перед деплоем в production!
